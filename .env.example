# App URL (used for frontend links)
VITE_APP_URL=https://your-domain.com

# Android APK URL (optional). If empty, frontend uses /downloads/pocketcraft.apk
VITE_APK_URL=/downloads/pocketcraft.apk

# Port (Hostinger will assign automatically, default: 3000)
PORT=3000
